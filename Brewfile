# Invoice Manager Dependencies
# Install with: brew bundle

# Qt6 framework
brew "qt"

# PDF text extraction tools
brew "poppler"

# XML parsing library for coordinate-based extraction
brew "pugixml"

# SQLite (usually pre-installed on macOS, but listed for completeness)
brew "sqlite"
